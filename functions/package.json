{
	"name": "goreach-functions",
	"private": true,
	"type": "module",
	"main": "lib/index.js",
	"engines": {
		"node": "20"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"lint": "tsc -p tsconfig.json --noEmit",
		"deploy": "cd .. && npx firebase-tools deploy --only functions"
	},
	"dependencies": {
		"firebase-admin": "^13.9.0",
		"firebase-functions": "^7.2.5"
	},
	"devDependencies": {
		"@types/node": "^22.19.19",
		"firebase-tools": "^14.13.0",
		"typescript": "^6.0.3"
	}
}
